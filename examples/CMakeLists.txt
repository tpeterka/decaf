add_subdirectory            (direct)
add_subdirectory            (datatypes)
if                          (lammps_example)
  add_subdirectory          (lammps)
endif                       (lammps_example)
