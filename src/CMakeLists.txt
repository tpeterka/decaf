if(${build_bredala})
	add_subdirectory    (bredala)	
endif(${build_bredala})
if(${build_manala})
	add_subdirectory    (manala)
endif(${build_manala})
if(${build_decaf})
	add_subdirectory    (decaf)
endif(${build_decaf})
